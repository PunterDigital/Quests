# PostgreSQL connection (matches docker-compose.yml)
DATABASE_URL="postgresql://quest:quest@localhost:5432/questtracker?schema=public"

# Optional: protect the /api/cron automation hook. When set, cron requests must
# send `Authorization: Bearer <CRON_SECRET>`.
# CRON_SECRET="change-me"
